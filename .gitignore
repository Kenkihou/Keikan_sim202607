node_modules/
.env
dist/
__pycache__/

# 練習用のサンプル図面（大きいので追跡しない）
05_roof-lab/sample/

# 04 の検証用データ。コードからは読み込んでおらず、大きい（87MB / 65MB）ので追跡しない。
# ⚠️ ファイル名で個別に指定している。*.splat / *.spz とまとめて外すと、
#   将来ほんとうに読み込むデータを入れたときに黙って抜け落ちる。
04_earth-simulator/kyoto_city_garden.splat
04_earth-simulator/export_25000_修正.spz

# 社内向けの操作ガイド一式（台本・画面・pptx）。
# ⚠️ このリポジトリは【公開】なので、社内資料は上げない。
#   フォルダは OneDrive の中にあるので、同期とバックアップはそちらに任せる。
01_building-builder/manual/

# 図面データ。内部でやり取りするものなので、ネットには上げない。
*.dxf
